class BronzeSaint extends Cavaleiro implements GoldSaint {
  protected String armadura;

  public BronzeSaint(String nome, String constelacao, String armadura) {

      System.out.println("O Bronze Saint " + bronzeSaint.getNome() + " executa " + golpes + " golpes!");
    }
  public String getNome() {
    return nome;
 }
}